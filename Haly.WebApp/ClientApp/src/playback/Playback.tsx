import { styled } from "../common/theme";

function Playback() {
    return <Wrapper />;
}

const Wrapper = styled("div", {
    background: "$black800",
    height: "$playbackHeight",
});

export default Playback;
